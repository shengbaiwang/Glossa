# Glossa 执行计划

## 当前目标

基础阅读和云端同步继续保留。根据用户 2026-09-10 的要求，原 AI 学习笔记先备份再退出使用，重构为“导读”。2026-09-11 用户另行授权独立右侧“对话”，以可控制的阅读上下文支持连续追问。当前 AI 范围为导读与对话，帮助读者组织内容、跨过难点并继续阅读；完整 AI MVP 和旧笔记附加功能不自动恢复。

## 对话方案（2026-09-11）

设计与标杆依据见 [对话设计](docs/design/conversation.md)。首个切片为桌面 EPUB，复用模型服务与来源定位；右侧采用“对话 / 导读 / 摘录”，工具栏和选区工具均有“对话”入口。桌面打开对话时正文与侧栏并排，保留调宽和关闭。

- 书名、作者、当前章节标题及阅读位置百分比始终携带，缺失时明确标为未知，不作为原文证据。默认共享当前页的实际可见文字，选区作为独立可移除附件；打开面板、翻页、选择范围均不自动调用模型。发送前可以展开核对本次原文。
- 上下文可选当前页、所选文本、一段、一节、一篇、一章、全书或关闭阅读上下文。一段从当前页涉及的原文段落中选择；节、篇、章均由读者明确选择原书目录条目及其结构范围，最多组合四个条目支持跨章提问，不凭名称猜测层级。扩大范围时提示可能包含未读内容。
- 章篇与全书按问题在本地关键词检索，宽泛问题使用分布式抽样；正文按结构保留，原文预算可选 2,000 / 4,000 / 8,000 字符，默认轻量 2,000；片段可逐条勾选、排除及跳回核对，不截断完整段落凑预算。元数据、指令、问题、最多四轮摘要另计输入，回答硬上限 16,384 token；界面明确说明，不虚报费用。全书为本地检索范围，不整本上传；取样结果明确标注“部分证据”，回答不能伪称读完整书。
- 答案按通过校验的完整短段流式呈现，支持停止、重试与连续追问。原文依据、解释推断、背景知识和证据不足分别标记；只接受本次来源 ID，来源正文从本地核对，支持跳回和返回阅读位置。
- 回答完成后保留本轮实际材料作为追问焦点，翻页更新书籍元信息但不自动替换焦点；可主动换用当前阅读内容或调整范围。每轮保存实际原文、书籍信息、预算、范围和摘要记录，可展开核对并重新使用。
- 历史按文档哈希独立保存在本机 `glossa-conversations`。每书最多 20 个对话、每个 40 轮；达到上限提示处理，不静默淘汰。模型只接收最近最多四轮、且模型及来源与当前授权范围一致的摘要；追问摘要可单独关闭；关闭共享后不通过历史重发已移除正文。旧 conversation-1 记录保持可读，新请求使用 conversation-2。
- 复用已有模型服务选择与安全凭据。关闭、切书、切离对话及更换模型时取消请求；历史加载失败禁止空值覆盖，保存失败保留可见结果并可重试。
- 验收覆盖默认未读内容排除、各级范围与本地检索预算、选区恢复、来源白名单、追问与取消、本机存储、浅深色/窄屏/RTL/电子墨水、相关测试、类型检查、lint 与隔离生产构建。真实阅读质量须另用真实服务验证，不由模拟响应测试推断。

## 导读方案（2026-09-10）

完整设计与取舍见 [导读系统方案](docs/design/reading-guide.md)，公开技能来源见 [调研记录](docs/research/reading-guide-skills-2026-09-10.md)。只借鉴三个机制，自行编写有边界的提示词，不引入外部技能执行器或依赖。

| 痛点 | 借鉴的必要机制 | Glossa 的实现与边界 |
| --- | --- | --- |
| 内容组织 | Deep Book Guide 的问题、依据与论证联系 | 先用本地目录定位章节；一次只为选定段落组生成少量关系说明，解释作者在解决什么问题、材料怎样支持观点。原文未给出的全书结论不补写。 |
| 难点解释 | Feynman 的机制先于术语、最小例子 | 最多两个必要难点，默认折叠。说清缺失的前提、概念或推理步骤；必要背景与解释推断独立标明，例子不冒充原书案例。 |
| 阅读节奏 | paper-reading-guide 的短导读、按需深入 | 选择章节 → 选择一段 → 主动生成 → 读短导读/按需展开难点 → 核对并回到原文。每次结束都交还控制权，不自动生成下一段。 |

### 范围与交互

- 首个切片为 EPUB。导读位于右侧 Notebook，与新增“对话”及“摘录”并列；普通高亮、摘录与手写心得保留。
- 打开导读只读本地目录；不预选章节、不提取正文、不发起模型请求。选择章节后才在本地提取，按段落、标题和结构边界形成可选阅读段；选中后展示范围与正文预览。
- 一次明确操作只发送所选一段及必要元数据。目标与上限均为 20,000 字符、最多 200 个来源；不可完整容纳的单个结构块不截断冒充完整来源，而是明确不可生成。不自动循环全章、不使用相邻未选正文、不按目录推测后文。
- 默认显示最多三条短导读与一条阅读提示；难点最多两项。每条原文观点或理解性解释必须关联已提供来源；必要背景只放在难点中并单独标注，来源标为相关原文，不充当背景事实的证据。
- 生成预算为 32,768 token；只有服务明确报告长度耗尽时，自动以 65,536 token 和更紧凑的输出要求重试一次。只重发同一所选段落，不追加残缺回答、不读取邻段，恢复过程可取消；第二次失败保留旧结果。GLM-5.3/Flash 官方接口使用受支持的低强度推理，未知兼容接口不猜测厂商参数。
- 模型服务沿用现有独立 provider 接口、多个自定义服务、连接检测和安全凭据存储。显示流式接收状态并允许取消，完整结果通过结构及来源校验后才呈现。关闭、切换摘录、书籍或阅读段时取消在途任务。
- 来源正文由本地读取，支持核对、跳转和返回实际阅读位置。错误及取消保留有效旧结果；保存失败保留可见结果并可重试，不显示虚假的保存成功。

### 数据与退回

- 先备份旧源码、相关未提交改动和未跟踪文件，记录原 HEAD、二进制 patch、清单与 SHA-256；退回仅针对已核验备份的旧 AI 笔记模块及其入口，保护无关改动。备份位置与完成情况见 `PROGRESS.md`。
- 停用旧全书框架、章节长笔记、自动分段统合、自测/作业、个人反思、版本列表与 AI 笔记导出；复用章节解析、模型设置和本地来源导航。
- 新导读使用独立 `glossa-reading-guides` 数据库，每本书/章节/阅读段只保留最新有效结果。旧 `glossa-study-notes` 数据库不打开、不升级、不清空，用户旧笔记数据留在原处。
- 缓存绑定文档哈希、所选正文与锚点、provider/地址/模型、提示词和结构版本；不同段落、文档或模型不混用。没有云同步、全书索引、外部检索或自动任务。

### 验收与真实效果

- 核验段落边界、来源白名单、输出限制、来源/推断/背景区分、缓存失效、空状态、缺失配置、生成/加载/保存失败、重试与取消。
- 原创 EPUB 在真实 Chromium 中验证“选择 → 生成 → 保存/重开 → 来源跳转/返回”，检查实际可见 CFI、未选正文不进请求、旧数据不变，以及浅深色、窄屏、键盘、RTL 和电子墨水。
- 相关与全量单测、类型检查、lint 和隔离前端生产构建通过；不操作用户书籍、真实密钥或已安装应用。
- 代码验收不等于导读效果已获证明。后续用陌生术语、背景缺口和论证跳步三类短文验证：读者能否解释原文关系、引用是否支持、能否顺利读下去以及是否增加阅读负担。暂不加遥测、测验产品或学习档案。

## 保留范围

- 本地书籍导入、书库管理，以及现有引擎支持的 EPUB、PDF 等格式阅读。
- 翻页、目录、搜索、阅读进度、字体排版、主题和无障碍阅读设置。
- 书签、高亮、手写笔记、笔记导出和跳回原文。
- 账号、书籍/进度/批注同步，以及 Readest Cloud、WebDAV、Google Drive、OneDrive、S3、iCloud 的现有实现。
- 同步所需的凭据保护、加密、冲突处理、云空间管理与既有服务权限判断。
- Glossa 独立应用身份、图标、数据目录和本地构建入口。

## 删减范围

此前删除的通用 AI 问答/解释、旧聊天历史、翻译、词典查询、Word Lens、朗读播放器与引擎、速读、段落辅助、校对、阅读统计采集、RSS/OPDS/网文入口、书籍公开分享、遥测，以及 KOReader/BookOrbit/Readwise/Hardcover/Discord 功能继续停用。对应 API、依赖、测试、评测数据与文档随功能删除。当前明确保留的 AI 范围为导读、新建的有边界阅读对话及其模型配置。

## 兼容边界

复用 Readest 阅读引擎与云同步协议。已导入书籍及其旧配置可继续打开；被移除的快捷操作回退到基础工具栏。旧 AI 笔记载荷作为不透明兼容字段保留，不主动删除用户已有本地数据。

本轮不更改原生平台插件、数据库迁移、云端既有记录或共享同步协议；它们仍包含少量历史功能兼容代码。原生插件进一步裁剪需要按平台构建与回归验证，不能仅凭前端无调用删除共享桥接实现。

## 验收

1. 书库和阅读页面仅呈现保留功能，旧设置不会激活已移除操作。
2. 目录、搜索、书签、高亮、笔记和进度恢复保持可用。
3. 云同步的上传/下载、配置与冲突处理回归通过。
4. 类型检查、lint、相关测试和前端生产构建通过；环境或既有失败必须如实记录。
5. 不更改现有用户数据，不覆盖任务开始前的工作区改动，不发布应用。

## 视觉与交互完善（2026-09-08）

以 Glossa 应用图标的黑白对比、圆角笔画和行间注释结构建立统一视觉系统。参考 Apple Books 的书库层级、Bear 的文字留白、Things 的控件细节及 Readwise Reader 的正文与批注关系，落实到现有基础阅读与同步范围。

- 默认主题使用纸白与墨灰，统一图标、圆角、间距、按钮层级、焦点及状态表达；保留深色、自定义主题、RTL、电子墨水与减少动效支持。
- 书库明确“继续阅读 → 全部书籍”的层级，改善书名、作者与进度的可读性，保留搜索、分组、导入、选择及同步操作。
- 阅读页保持正文优先，统一目录、页码导航、书签和批注侧栏；保留阅读引擎、已有用户排版设置及原文定位。
- 验收覆盖桌面和窄屏视觉、键盘及触控目标、阅读核心流程与相关回归检查；功能范围服从当前目标。

## 阅读配色（2026-09-09）

参考 Dark Reader 的柔灰纸面与炭黑夜色，重整内置主题为低饱和、明确对比的成对浅深配色，保留 Glossa 默认纸白/墨灰。以真实文字预览和可选择的预设呈现配色；常用方案优先，更多颜色可展开，自定义方案从当前配色开始。选择沿用既有主题持久化与系统明暗跟随，旧主题标识及用户自定义颜色保持兼容。

- 以 App 图标的圆角纸片、墨色和留白呈现配色：20px 圆角色卡居中字样、名称置于卡片下方，细轮廓与反色圆勾表示选中，轻微纸面层次提升质感；去除装饰横线与预览下划线。通用控件保留 12px 圆角，键盘、窄容器、RTL 和电子墨水可用；电子墨水关闭阴影和过渡。
- 内置正文、链接与次要文字经过对比度检查；不把配色偏好宣传为医学护眼效果，不引入滤镜或外部样式依赖。
- 自定义浅深方案可预览、保存与删除，取消不应用；改名保持原标识，同名冲突明确提示，保存失败保留草稿与已有偏好。

## macOS 界面适配（2026-09-09）

用户选择让界面更像 Mac 原生应用。保留 React/Tauri 与阅读、同步实现，在桌面宽度采用紧凑统一工具栏、系统界面字体和可收起的书库分类侧栏；阅读侧栏统一列表选中态与分段按钮。复用系统红黄绿按钮及现有拖动、全屏逻辑，不模拟窗口按钮。窄屏、RTL、电子墨水、深色与自定义阅读字体保持可用。验收包含分类导航、搜索快捷键、侧栏收起、阅读与批注流程及响应式布局。

## Glossa 品牌与上游归属（2026-09-09）

用户可见的产品名称、图标、窗口与安装元数据、帮助/反馈入口和导出署名统一为 Glossa。保留 Readest 原始版权、AGPL-3.0-or-later 许可证、第三方声明与 Git 历史；在关于页和仓库说明衍生关系、修改日期并提供 Glossa 源码与离线许可证。独立版本不得使用 Readest 的安装包或更新源。

实际 Readest Cloud 服务及其条款、存量本地/云数据路径、同步与 OAuth 协议标识、依赖名和历史工程路径保留准确含义；不为名称替换移动或删除用户书库数据。当前验收仍以本地 macOS 与 Web 为主，不发布应用，也不将上游移动端商店配置当作 Glossa 已上线版本。

系统层身份包含开发启动实际执行的二进制名称和安全存储条目名称。桌面安全存储以 Glossa 命名，正式版、开发版及测试身份隔离；旧 Readest 条目仅在新条目不存在时按需读取，复制成功后使用新条目，保留旧值。清除操作须防止旧值被再次恢复，权限拒绝或复制失败不得当作凭据不存在。已有书库目录、同步路径与账号协议保持不变；产品界面及导出入口使用 Glossa 名称，并继续兼容旧批注格式。

## 阅读交互与品牌图形（2026-09-09）

从 App 的圆端墨线和行间结构延伸可复用的 Glossa 矢量图标，统一书库导航、阅读工具栏、目录与笔记入口。阅读工具按位置与用途分组：返回与浏览在前，排版、书签、笔记在后；低频选择偏好和单书关闭操作通过有文字的菜单进入。侧栏在书籍身份下方直接切换目录、笔记和书签；切换无人工延迟，支持键盘导航。进度条拖动预览、松手定位并可取消，翻页与历史/章节导航分层。保留原生窗口行为、正文排版与触控边界，验收浅色、深色、窄屏、RTL 和电子墨水。

## 常见阅读字体（2026-09-09）

内置字体库改为常见系统字体预设：中文宋体、楷体、仿宋与黑体；西文 Times New Roman、Georgia、Arial、Helvetica 等；日文明朝与ゴシック；韩文 바탕 与 맑은 고딕；阿拉伯文 Traditional Arabic 与 Tahoma。以本地字体别名和通用字体回退适配平台，不捆绑商业字体、不自动请求外部字体服务。仅为缺少系统楷体的设备随应用附带 Arphic Public License 授权的 AR PL UKai CN 后备字体，保留完整字形、许可证与转换说明。默认按文档语言区分简繁中文、日文、韩文与阿拉伯文。保留系统字体扩展列表、自定义字体导入和删除功能；仅从 Glossa 字体候选及阅读字体链中排除用户指定的 100-SS Xian Song Ti、173-SSShanShuiSongTi、波本威士忌、寒蝉锦书宋、三极花朝体及其变体，其他系统与自定义字体保持可用。指定旧字体和旧内置默认值回退到常见预设；删除正在使用的导入字体时重置相应选择，失败时保留字体并反馈。保留共享同步数据协议，不卸载操作系统字体。验收包含字体扩展、导入/选择/删除/失败/取消、语言回退、旧配置与离线渲染。

## 字体配置交互（2026-09-09）

借鉴 Apple Books 的选项分层、Kobo 的阅读页调节和 Readwise Reader 的直接操作，重构现有字体配置。沿用 Glossa 的纸白/墨灰、主题变量、圆角及简明按钮，以样句体现字体差异，避免解释性长文。

- 主层按文档语言提供统一字体列表，“原书字体”与具体字体在同一列表选择，不再独立切换原书/自选。提供字号与 100–1000、步长 1 的字重滑杆和精确数字调节；字体列表原地展开、搜索并展示统一样句。“更多”仅保留对代码阅读有实际用途的等宽字体，保留字体管理和对应设置搜索入口。
- 根据用户后续精简要求，移除最小字号的界面、搜索入口和排版作用；旧字段仅为书籍/同步兼容保留，不自动清除或重写。移除三档字重按钮、独立衬线/无衬线类别切换及重复字族选择；通过一个正文字体入口直接选择，继续使用已保存的字族偏好，避免升级改变现有排版。
- 中文样句为“微雨从东来，好风与之俱”，西文为“Sunt lacrimae rerum et mentem mortalia tangunt.”；繁体界面对应繁体字形，其他文字采用同样诗意的原创短句。
- 桌面宽屏阅读设置所有标签页共用右侧面板（RTL 随方向镜像），切换字体、排版、主题、行为、语言、云同步、模型服务与无障碍设置时保持位置和尺寸一致；窄屏沿用响应式弹层。保留正文可见区域；调整沿用现有即时排版与本书/全局保存协议。打开面板不重复保存未改变的设置，保留导入字体、删除回退和旧字体迁移。
- 验收包含选择/搜索/返回/重置、错误重试、保存范围、外部字体变化、键盘、深色、窄屏、RTL 和电子墨水；不改变阅读引擎、用户书籍或安装版应用。
