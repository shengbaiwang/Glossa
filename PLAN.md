# Glossa 执行计划

## 当前目标

基础阅读和云端同步继续保留。AI 导读保持完全移除；对话目标是以引用开关选择全书自动取材的阅读 harness 或基础模型对话，导图继续基于明确选择阅读段生成。导图聚焦简洁、统一的编辑和原文核对，不恢复旧导读、AI 学习笔记与完整 AI MVP。

## 阅读 harness 与引用两态（2026-09-22，最新目标）

- 对话只提供引用开/关；开启时当前全书是可查阅范围，模型与 harness 自行判断问题需要的章节、段落与背景材料。关闭时使用既有基础对话：书名、作者、目录路径与有界普通历史，不提供书内读取工具或自动正文输入。引用开关控制访问权限，不只是显示样式。
- 移除对话中的手动页/选区/阅读段范围选择要求；用户自然语言指定的章节、文段及比较对象由 harness 解释为本次任务目标。旧固定附件和来源版本保留可读，历史按模式/依据隔离，不能把旧原文自动注入关闭引用的请求。
- 模型负责理解问题、选择必要材料与提出补查；harness 提供目录/本机搜索/读取能力、上下文组装、来源验证、预算、取消与恢复。流程围绕问题需要组织材料，保留章节与来源角色线索，对候选做相关性筛选；证据不足时允许有限补查，不能只在零命中时补查。一般问题保持短路径，完整总结继续完整覆盖。
- 只在发送问题后按需工作；不无差别上传全书，不增加用户配置的 Skill 或问题类型选项。继续沿用有限调用/时间/字符预算、合法来源、本机核验与跳转、隐私及原有未授权功能边界。
- 两态已经接入会话存储；旧固定附件会话未显式设置时保持关闭引用，主动开启才转全书新请求。指代当前文段的问题在发送时冻结选区/可见页，按需本机核验读取，不需要范围菜单。
- 普通问答首批最多 8,000 字符，提供章节/标题线索；首次回答可以直接完成或主动补查，至多两轮/四次工具、含恢复至多五次模型请求，120 秒与累计 30,000 字符/180 来源限制。工具不支持时才回退一次取材计划。未列目录的序言/附注可读；详见阅读 harness 规格。
- 验收包括：引用开启后自动跨章取材；关闭后无自动正文读取/模型工具调用/旧材料混入；不同类型真实问题的关键证据召回、结论支持度、针对性与耗时。模拟测试与真实模型质量分别评估，不以单本书特例作为通用修复。

## 全书阅读对话（2026-09-22，实现基线）

用户明确授权朝 NotebookLM 的核心阅读对话体验推进，包括默认全书查阅。规格见 [阅读 harness](docs/design/reading-harness.md)。下列为两态目标之前的实现基线，手动范围选择及仅零命中扩展查询由上述最新目标取代；其余来源、预算与恢复规则继续有效。

- 可重排 EPUB 默认以当前全书为可查阅范围，含未读章节；打开面板、翻页和切范围不调用模型、不预读全书。输入框显示「全书」，范围菜单可引用当前页、选区或章节阅读段。明确附件保持固定快照且约束全部读取；移除后回到全书，新会话默认全书。其他格式继续使用原基础对话。
- 具体问题优先本地 BM25 检索后直接流式回答，不固定先请求模型规划；无命中时最多一次查询扩展规划，最多四个互补短语，不能将普通问答升级成逐批全书总结。点名章节保持章节边界。选取几个精彩观点时提供跨书籍 spine 的有界样本，不冒充穷尽性排行。全书检索包含前言、非线性附注，中文分词与双字词共同召回，保留相邻限定语。
- 全面总结、观点梳理与章节比较采用完整范围读取；先逐段整理保留显式分论点、限定条件及前后补充观点的来源清单，再汇总。程序核对每批来源 ID 覆盖和清单来源白名单；最终回答缺少清单对应引用时最多补查改写一次，仍缺失则报告未完成。该检查不能自动证明观点解释或语义覆盖正确，真实模型质量需独立评估。
- 全书权限与正文证据分开：会话保存轻量权限记录，每个回答版本保存发送给最终回答模型的完整原文来源及读取覆盖信息；会话不保存全书正文副本或索引。独立的本机内存缓存按打开的文档对象、哈希和解析/索引版本隔离，按需复用原文块和 BM25 索引，文档关闭释放后或应用重启不保留。观点清单缓存还绑定原文/锚点、范围、问题、模型配置与提示词版本，只缓存已校验结果；失败重试可复用已完成批次。历史只使用当前书籍、同范围的完整轮次，所需原文重新本机核验，改动/失效/旧范围材料不进入请求。旧附件与旧历史继续可读，不迁移或清库。
- 默认查询/总结为有限计划流程：具体问题来源初选最多 18,000 字符；完整总结原文累计最多 240,000 字符、24 批，通常每批 12,000 字符/100 来源，单个语义块最多 20,000 字符；最终证据最多 80,000 字符/1,000 来源。全书具体问答总时限 120 秒，总结 8 分钟；单次任务最多 36 次模型请求（含恢复），可取消、丢弃迟到结果；超限或读取失败不降级成假完整总结。固定范围普通问答保留三次工具/四次请求/90 秒，固定范围总结采用同一观点清单流程。
- 引用延续行内本地编号、悬停/聚焦原文、本机核验跳转、临时高亮及返回阅读；点击角标直接跳到原文，不在对话底部常驻原文/补充说明框，切换来源直接点击对应角标。模型不能创造来源 ID 或位置。UI 保持纸墨主题与窄栏布局，只在等待时显示读取/整理状态，不加入教学式说明。
- 回答末尾以可悬停/聚焦/点击的用量摘要取代固定原文依据文案；按回答版本保存实际 Token、各请求输出上限与耗时，包含隐藏步骤及恢复。缺失字段隐藏、旧记录无用量时隐藏入口，零值保留；主数据卡片与双列明细分层，输出预算/请求数默认折叠，部分用量明确标下限；来源核验与行内引用不变。端到端吞吐与输出预算不冒充服务端 TPS 或实际费用。费用只采集接口返回的 `usage.cost`，包含本次分段/恢复并按版本保存；缺项隐藏、部分费用标下限，未知币种不猜美元，不按 Token 推算，不额外查账单，详见对话设计。
- 规划首试预算 4,096 tokens、长度截断可重试一次 8,192；清单首试 16,384、截断只拆分失败批次最多两层，恢复批次 32,768。最终回答首试遵从用户偏好或默认 8,192，最多续写/重试两次，逐次增加预算且不超过 65,536；不将用户的回答偏好覆盖到隐藏规划/清单。终止事件里的正文先显示，部分回答不因恢复被清空。
- 验收以原创 EPUB 覆盖默认全书、跨章检索与跳转、四个显式论点及结语、分批覆盖、来源伪造拒绝、历史/版本恢复、缩小范围、取消/超时和预算不足。模拟模型通过只证明工程链路，不宣称已与 NotebookLM 的实际回答质量持平。不新增网络搜索、向量库、多 Agent、后台自主研究或写入工具。

## 深色模式原书文字颜色自动校正（2026-09-15）

可重排 EPUB 在深色主题下自动校正原书文字颜色，保留原书的颜色层次。覆盖书内样式表（含外部 CSS）、内联 `style` 与 `<font color>` 三个表面；只调整显示，不修改书籍文件、正文节点结构或批注定位，固定版式保持既有行为。

所有校正一律输出 `light-dark(原色, 校正值)`：浅色分支保留原书颜色（浅色下恢复），深色分支生效，借助阅读页已在正文 iframe 上设置的 `color-scheme` 随主题即时切换，不重载书籍、不重跑变换。不支持 `light-dark`/`color-mix`/oklch 的引擎丢弃声明并回退为可读的主题前景；`<font>` 保留原 color 属性作为回退。

黑、深灰文字转为主题前景与背景混合的暖白/浅灰，混合比例随原始亮度保序：正文最强、注释稍弱但仍清楚可读（对比度门槛 4.5）。绿、蓝、红等彩色文字在 oklch 中与白色混合，保留色相、提升明度并柔化彩度，成为适合深色背景的柔和亮色。只有对比度不足的原色才被校正，已可读的颜色保持原样。

带底色的文字按实际背景校正：同一规则/同一内联属性内的浅色背景同步包为深色（前景背景一致切换）；保留的深色背景按该背景计算对比度；中间调、半透明、图片或渐变背景上的文字不做提亮，避免提亮后反而看不清。跨元素的底色关系（父元素底色、子元素文字）静态不可见，保持保守不处理。图片、SVG `fill`、`border-color`、`-webkit-text-fill-color` 不在本轮范围；替字图片仍由既有专用处理负责。

## EPUB 替字图片主题适配（2026-09-15）

可重排 EPUB 自动适配正文中的小幅黑字白底替字图片：结合相邻文字（含 font/span 包装）、原始/显示尺寸和本机像素特征保守识别，将白底转为透明、字形着色为主题正文墨色，随主题切换更新，不依赖全局图片反色开关。彩色、透明、大幅、独立插图及链接图标保持既有处理，无法读取像素时保留原样。处理不等待网络、不阻塞页面加载、不修改书籍文件、图片地址、正文节点或已有定位；不做 OCR、不猜测字符、不承诺修复原始低分辨率和字体差异。固定版式与其他格式保持既有行为。

## 书内搜索：单层选项与统一命名（2026-09-15）

搜索工作区遵循 Glossa 纸墨界面，用户可见搜索操作统一使用「搜索」。输入框固定为「搜索书内文字」，下方直接切换「全书 / 本章」，入口固定为「选项」。单层菜单按「模式」集中普通、全词、邻近、正则四选一，下方为区分大小写与区分变音符号独立开关；删除高级页及重复全词开关。邻近距离仅在该模式启用时于同一菜单内显示 5/10/20/50 词；非默认设置可恢复默认。保留既有搜索能力、配置兼容及本机历史。

菜单选中沿用左侧轻墨底色，悬停不覆盖选中底色，不加白色外框；键盘焦点使用细下划线，保留单选/复选标记、禁用语义、方向键与 Escape 返回入口。共享菜单状态写入 DESIGN.md 与 glossa-foundations.css，以便其他菜单复用。

本章以当前最深目录项至下一同级/上级项的原文范围为准，包含该项下的子项，可跨 EPUB 文件，并裁剪同一文件中的相邻章节。搜索发起时固定范围，点击命中不重新划定本章。缺失目录时使用当前节；有目录但位置不可确定时提示重试，不默默扩大为全书。索引仍使用既有本机搜索服务。

中文输入法确认后才启动搜索；新输入、配置变更、清空、关闭和卸载取消旧请求及等待任务，旧结果/错误不进入新查询。状态区区分搜索中、停止、失败和结果上限。结果采用轻量章节列表、原文片段与纸墨高亮；浮动导航统一显示当前命中/总数、逐条切换、列表和关闭，以及返回搜索前位置。输入框 Enter / Shift+Enter 在已有结果时切换前后命中，无结果时立即执行搜索。保持窄栏、RTL、深浅色和电子墨水可用，不改动书籍、批注、同步协议或 AI 范围。

## 书签负责定位（2026-09-15）

书签仅用于保存阅读位置并快速返回，笔记负责记录。书签列表采用紧凑两层排版：首行章节与页码，下方两行只读文段预览；以留白区分条目、减淡状态底色，支持跳转与删除。右下角使用直接删除图标，与文段底部对齐、不另占一行，桌面悬停或键盘聚焦时显示，触屏保留可达操作。移除编辑、复制及左侧重复添加入口。正文顶栏保留添加/移除书签，左侧列表使用叠层书签图标，添加使用带加号的单枚书签，已保存为实心状态。沿用 Glossa 纸墨主题、紧凑字层、留白和键盘焦点，不添加解释性注释。旧书签记录、定位、保存与同步协议保持兼容，不主动清理旧数据。

页面右上角已保存书签使用内收的小幅暖红色丝带，避开页面圆角；形状、颜色与下拉书签预览一致，兼容顶部安全区，电子墨水使用主题墨色，不拦截阅读操作。

## 界面基础规范（2026-09-14）

文字、间距、交互状态、菜单/弹窗和表单按功能角色复用：完整页面、设置表单与紧凑阅读侧栏保持不同密度。共享字号为页面标题 26px、弹窗标题 16px、集合标题 15px、界面正文/菜单 14px（窄屏 16px）、紧凑工具 13px、辅助/分组文字 12px；设置行至少 56px、导航行 64px、菜单行 36px、标准字段 36px，触屏控件至少 44px。面板/弹窗内边距 16/24px，菜单/弹窗圆角 12/20px；颜色来自主题，键盘焦点可见，选项有真实选中语义，禁用动作使用原生 disabled。正文、字体预览、用户字号、窗口/安全区域及专用控件保持各自职责。实现复用 glossa-foundations.css 与既有组件，完整约定见应用 DESIGN.md。

## 图标层级（2026-09-14）

目录使用独立章节列表图标。Glossa 图标遵循共享 24 单位网格、1.8 单位圆端描边，同时按功能区区分视觉尺度：书库导航 20px、阅读顶栏与左右侧栏 18px、局部工具与菜单 16px、辅助标记 14px。书库主导航使用 12px 圆角，阅读主控件 11px，局部操作 9px，行内辅助 8px；局部纯图标目标为 28px、行内纯图标目标为 24px，触屏均保留至少 44px。功能图标统一从 GlossaIcons 引入，辅助图形以共享适配层协调 Lucide 的网格与圆端笔触，品牌标志、文件类型及语义状态图形保留明确例外。此规则覆盖书库、阅读、设置、批注、同步、弹窗与通知；左右栏入口共用组件，选中底块为 28×28px / 10px 圆角，桌面点击区 32×32px、触屏 44×44px；组内间距 2px（桌面中心距 34px），边缘留白 10px，侧栏开关与入口组至少间隔 6px，原生窗口按钮留白单独保留。左右栏使用同一主题墨色选中背景，悬停只改变图形颜色，避免与选中混淆。弹层、内容卡片、品牌图形及触控区域保留各自语义与尺寸，不对所有 SVG 或圆角做全局覆盖。规范与例外以应用 DESIGN.md 为准，验收包含左右栏协调、浅深色、窄屏、RTL 和电子墨水。

## 朗读、划词与品牌（2026-09-13）

按用户最新授权恢复基础朗读和划词能力。正文唯一更多菜单提供“朗读”，从当前位置沿用 foliate 分句/章节迭代；选区工具栏恢复词典、翻译与朗读，选区朗读仅消费所选文字。采用系统语音，紧凑播放器提供播放/暂停、声音、语速与关闭；旧语音偏好继续可读。查词恢复系统词典与已导入词典/原在线词典，设置“语言”页恢复词典管理；划词翻译直接使用 Google Translate，仅发送主动选择的文字，支持取消、失败重试与目标语言切换。

工具栏、播放器与查询浮层使用 Glossa 纸墨配色、共享线性图标、主题圆角与键盘焦点，适配窄窗、RTL 和电子墨水。原精简版默认工具栏恢复新增入口，自定义顺序与空工具栏保持用户选择。简单对话的元数据边界、右侧对话/导图/笔记及导读移除范围不变；不恢复旧 AI 面板、Word Lens、全文翻译或音频下载/付费服务。

用户可见应用品牌统一为 Glossa，实际第三方云服务采用中性云服务名称，保持真实链接与服务目的地。上游版权归属、许可证和来源保留在“开源许可证”中；旧本机目录、同步字段、协议标识和后端域名保留兼容，不能以改名导致旧书籍或同步数据失联。

## 阅读菜单统一（2026-09-13，图标规范补充于 2026-09-15）

阅读更多菜单的操作项与子菜单入口统一固定前置功能图标，复用 GlossaIcons 的 16px / 1.8 描边规范；开关状态单独用尾部勾选，快捷键与展开箭头各守其位，禁用整行减淡。纯单选/复选列表保留状态标记，无需每个选项另配功能图标；书籍选择列表保留封面例外。具体复用约定写入 DESIGN.md。

左侧移除常驻封面、书名、作者信息区；书名仅保留正文顶栏。现有更多菜单提供「书籍详情」，主动打开后复用详情弹窗显示封面、作者及其余元数据，绑定所在正文书籍与最新阅读进度。

左侧不再保留更多菜单；书籍与阅读操作合并至中间正文顶栏右上角、Aa 右侧的唯一“…”入口。书签与 Aa 保持直接可达；合并菜单中的书籍操作明确绑定该正文的 bookKey，侧栏关闭或多书阅读时仍可正确使用。

## 笔记统一到右侧（2026-09-13）

左侧仅保留「目录 / 书签」与书内搜索；目录、书签和搜索是互斥入口，采用共享 Glossa 线性图标，与右栏统一悬停名称、浅墨选中底色和键盘切换。左栏展开时开关位于其顶栏最左侧，后接紧凑排列的目录、书签、搜索，重复点击已选入口保持打开；搜索快捷键直接选中搜索，Escape 返回此前目录或书签，不改变旧书籍配置。收起后开关位于正文顶栏最左侧、书库之前，展开时正文不重复。右侧为「对话 / 导图 / 笔记」。笔记工作区集中原有高亮、批注和摘录，复用批注章节列表、筛选、条目内编辑与原文跳转；统一搜索。旧左侧笔记标签回退目录，批注浮层和底栏笔记入口转向右侧。仅调整入口与展示，保留原本存储、同步与旧数据。

## 导图生成与基础编辑（2026-09-13，最新范围）

用户重新授权 AI 生成，并要求与基础编辑、原文跳转及文段显示整合。取代当日早先独立手工工作区和生成停用的安排。详见 [导图设计](docs/design/mindmap.md)。

- 右栏以同一层级的「对话 / 导图 / 笔记」图标入口组织，悬停显示名称；导图内部仅以轻量分段按钮切换大纲和导图。使用 Glossa 纸墨主题、共享线性图标、细连线及一致的间距、圆角和焦点；移除教学式小字与重复标题层，不使用彩虹分支、装饰卡片、来源角标。
- 打开只读本机导图。用户明确选择章节和阅读段，再主动生成；保留范围首尾原文与模型选择，单次最多 20,000 字符/200 来源，不自动读整书、邻段或后文。UI 通过现有 provider 调用，关闭、换段、换模型中止在途工作，迟到结果不得进入新状态。
- 优先生成 5–10 个短观点、2–4 条主分支和 2–3 层，硬上限 24 节点/4 层；保留限定条件、例证边界和有原文支持的关系，拒绝来源白名单之外的内容与无证据假图。来源合法不等于语义已证实，真实生成质量仍需实际阅读审阅。输出上限遵从用户模型设置。
- 有效结果直接进入同一可编辑工作区；旧生成图可主动建立编辑副本。大纲与水平导图共用一棵有序树，支持文字修改、子级/同级、升降级、排序/删除、折叠和会话内 50 步撤销/重做。每书最多 20 图、每图 200 节点/12 层；空白手工图仍可独立使用。
- 有来源节点单击核验并跳转，底部显示本机对应文段；多来源可前后切换，可返回阅读位置。双击、F2 或直接编辑按钮修改，来源原始快照独立保留；改写显示“已编辑”，推断显示“理解性解释”，这些是内容身份而非使用说明。新手工节点无来源身份；核验失败不跳转，仅展示明确未核验的保存文段。
- 常用编辑及撤销置于工作区，导图级操作与观点级操作分开收在两个菜单中；搜索、按层展开、聚焦、扩展画布、缩放和 JSON 备份能力继续保留，不新增复杂操作。适配窄栏、RTL、深色、电子墨水与中文输入法。
- 独立 `glossa-map-workspaces` 保存编辑树、原始生成依据和视图状态；原 `glossa-reading-mindmaps` 保留兼容记录。结构、书籍身份、来源关联和内容/缓存哈希在运行时校验，旧 mindmap-1 与新 mindmap-2 均可读。串行原子保存、跨窗口修订号比较、加载失败禁止覆盖、写入失败保留修改重试；关闭侧栏后保存队列继续。
- JSON 导入新增副本而不覆盖；跨书导入移除来源身份。不会主动清理旧本机数据，不新增同步、协作、图形依赖、完整正文检索或其他 AI 功能。

## 基础对话方案（2026-09-12，正文边界已由 2026-09-21 授权扩展）

此节记录无原文附件时仍使用的基础对话行为；原「禁止正文与检索」条款已由上述 2026-09-21 范围取代。用户此前退回上下文体系，优先完成自然、方便的基础对话。基础规格见 [对话设计](docs/design/conversation.md)，取代 2026-09-11 范围/材料方案与 2026-09-12 早先研究建议。

- 每次请求自动附带书名、作者和当前完整目录层级路径，不含进度百分比、定位或正文。缺失值为空，不猜测。打开、翻页、选字和切换会话不自动生成回答；移除选区对话附件入口及其自动监听。
- 直接通过现有 provider 调用兼容 API，普通 user/assistant 消息承接追问，逐文本片段呈现 Markdown。默认简洁，按问题需要展开；不强制输出 JSON 段落、证据类别或固定回答模板。无正文时不伪称看见页面、核验引文或完成检索。
- 对话不内置提示词预设（2026-09-12 用户明确要求删除）。用户可在设置“对话”页自建多条提示词（名称＋内容，本机保存，最多 20 条），输入框旁的选择器在已有提示词时出现并可随时切回“不使用提示词”；选中的提示词作为唯一 system 消息随请求发送，未选择时请求只有元数据与普通聊天消息。界面不加入解释功能的注释性文案。旧实测提示词与调优记录保留为研究历史，不自动恢复为默认。
- 主界面为顶部会话切换弹层（最新在前，标题会话附首问预览）、新建/删除图标、消息列表与固定输入框；不展示材料、预算、摘要、提示说明和键盘注释，也不显示自动附带的书名、作者与目录路径。纸墨色、Glossa 图标、主题圆角与阅读侧栏一致，支持窄屏、RTL 和电子墨水。
- 输入框内直接选择服务/模型，可搜索服务提供的模型列表或手动输入名称；保留模型设置入口及现有安全凭据。切模型中止旧请求，完整聊天历史继续可用，不按模型清空对话。只在打开选择器时请求模型列表，列表失败不阻塞手动选型。
- 支持新建、切换、删除本机对话、复制、重新生成最后回答、停止及失败重试；Enter 发送、Shift+Enter 换行、IME 防误发。停止保留已到达文本；关闭、切会话或切模型后迟到结果不得污染新状态。历史加载失败不能覆盖，保存失败保留结果并可重试。
- 每个回答保留版本：重新生成、或编辑最后一个问题后重发都新增版本而不覆盖旧答案，可在版本间切换，后续请求只发送当前选中版本；每轮最多 8 个版本，达到上限明确提示删除后重来。会话首条回答完成后自动生成名称（一次独立小请求，失败保留问题标签，用户手动命名优先），仍可手动重命名（最长 120 字符），并可在当前书籍内按名称、问题与回答搜索历史。问题上限由 2,000 提升到 20,000 字符，输入框随内容自动增高、接近上限时才显示字数（2026-09-13 起移除展开按键）；请求与存储校验同步。
- 模型设置按服务与模型的能力提供推理强度与输出上限，不支持时不显示且不发送厂商参数；推理强度同时放在输入框右下角直接调节，选择即保存到当前服务；对话默认 16,384 token 输出，用户设置优先，持久化到本地时只保留服务身份。回答支持数学公式、代码块复制与可调字号；字号放入顶部对话菜单，与重命名及删除共用入口，不设独立字号按钮或设置页，继续禁止原始 HTML 执行与远程图片自动加载。问题与回答文本均可直接选中复制；重新生成使用统一的圆形回转图标。
- 复用 `glossa-conversations`，每书最多 20 个会话、每会话最多 40 轮，达到上限引导新建/删除，不静默淘汰。新请求 conversation-3；仅带当前会话最近最多 12 轮完整消息，总历史最多 48,000 字符，按完整轮次收束，不摘要。未完成回答不作为后续模型消息。旧 conversation-1/2 历史保持可读，不重放旧材料。
- 验收覆盖 API 消息边界、无正文读取、会话隔离、模型切换、流式与取消、Markdown 安全、回答版本与编辑、模型参数能力、输入上限、历史重命名与本书搜索、数学公式与代码复制、本机存储、主题及窄屏；导图与基础阅读回归不受影响。模拟服务验证工程行为，真实回答质量仍需实际使用验证。

## 历史导图生成方案（2026-09-12，现由上述统一工作区取代）

用户授权借鉴文本转导图项目中有价值的机制，开发 Glossa 自有右侧导图。规格与依据见 [导图设计](docs/design/mindmap.md)。

- EPUB 阅读右栏增加“导图”，桌面与正文并排；点击分支文字直接跳到经本地核验的对应段落所在页，保留返回原位。展开按钮只展开/收起，不触发导航。
- 图面沿用纸墨配色、主题圆角、细连线与留白；无来源编号、角标或解释卡片。理解性解释在节点悬停提示中明确标注。默认展开首层，可逐层展开、全部展开/收起及调整字号；适配窄栏、键盘、RTL 和电子墨水。生成后的范围与模型控件收起。
- 借鉴 Markmap 的层级与折叠、Mind Elixir 的节点交互、ebook-to-mindmap 的分段生成与本地重开，以及 mindmap-skills 的短标签原则。自有 React/CSS 渲染，不增加图形依赖或外部技能运行器；不恢复全书生成、测验、笔记导出或完整编辑器。
- 使用独立阅读段模块的显式章节/阅读段选择、来源提取、provider、取消及导航。一次仅发送所选完整阅读段，沿用 20,000 字符/200 来源边界，不使用邻段或后文，不改变简单对话的元数据边界。
- 输出以核心问题为根，用明确关系连接关键概念；优先 6–12 节点，上限 24 节点/4 层，每节点 1–4 个已提供来源，首个来源作为直接跳转目标。结构、来源白名单与缓存身份都需运行时校验；证据不足时不生成假图。
- 独立本机 `glossa-reading-mindmaps` 数据库，每书/章节/段保留最新有效图；缓存绑定正文/锚点、文档哈希、模型/地址/服务与版本。加载失败禁止覆盖，保存失败保留可恢复结果，关闭/切换取消，旧导读和旧笔记数据库不迁移。
- 验收包含结构与来源安全、请求边界、失败与取消、真实 EPUB 的直接分支跳转/返回、IndexedDB 重开与主题布局。学习研究支持概念关系组织的价值，不等于证明 AI 生成图的准确性、记忆收益或速度；后续以真实短文理解任务验证，不加遥测或测验产品。

## 导读移除（2026-09-13）

删除导读标签、生成与缓存协议、提示词、专用展示和存储实现。右侧默认打开对话；导图继续使用独立阅读段模块及本地来源校验。旧导读数据库不再打开、写入或迁移，不主动清除用户本机数据；不恢复旧 AI 笔记。

## 保留范围

- 本地书籍导入、书库管理，以及现有引擎支持的 EPUB、PDF 等格式阅读。
- 翻页、目录、搜索、阅读进度、字体排版、主题和无障碍阅读设置。
- 书签、高亮、手写笔记、笔记导出和跳回原文。
- 账号、书籍/进度/批注同步，以及 Readest Cloud、WebDAV、Google Drive、OneDrive、S3、iCloud 的现有实现。
- 同步所需的凭据保护、加密、冲突处理、云空间管理与既有服务权限判断。
- Glossa 独立应用身份、图标、数据目录和本地构建入口。

## 删减范围

此前删除的通用 AI 问答/解释、旧聊天历史、Word Lens、速读、段落辅助、校对、阅读统计采集、RSS/OPDS/网文入口、书籍公开分享、遥测，以及 KOReader/BookOrbit/Readwise/Hardcover/Discord 功能继续停用。对应 API、依赖、测试、评测数据与文档随功能删除。当前 AI 入口为基础对话及所选阅读段的导图生成；导图编辑与原文核对按上述范围整合。

## 兼容边界

复用 Readest 阅读引擎与云同步协议。已导入书籍及其旧配置可继续打开；被移除的快捷操作回退到基础工具栏。旧 AI 笔记载荷作为不透明兼容字段保留，不主动删除用户已有本地数据。

本轮不更改原生平台插件、数据库迁移、云端既有记录或共享同步协议；它们仍包含少量历史功能兼容代码。原生插件进一步裁剪需要按平台构建与回归验证，不能仅凭前端无调用删除共享桥接实现。

## 验收

1. 书库和阅读页面仅呈现保留功能，旧设置不会激活已移除操作。
2. 目录、搜索、书签、高亮、笔记和进度恢复保持可用。
3. 云同步的上传/下载、配置与冲突处理回归通过。
4. 类型检查、lint、相关测试和前端生产构建通过；环境或既有失败必须如实记录。
5. 不更改现有用户数据，不覆盖任务开始前的工作区改动，不发布应用。

## 阅读界面层级统一（2026-09-13）

阅读页收敛为「一条顶栏＋左右侧栏」。参考 Obsidian 的顶栏分区：左侧为返回书库、目录（左侧栏）图标，书名保留原有居中纯文字样式，不增加页签底框；书签、阅读设置与整本书「更多」位于右侧，右侧栏开关位于末端。左侧栏只承担书内导航（目录、书签与明确属于当前书的搜索）；右侧栏只承担辅助阅读，关闭时由顶栏末端图标打开，打开后在与顶栏对齐的 44px 头部显示「对话 / 导图 / 笔记」紧凑图标组（悬停名称、键盘和无障碍标签），收起按钮移至右栏最右端；当前图标用单一浅墨色底块强调，不再设通用「…」菜单，新建对话、生成导图等操作收在对应标签内部。右栏移除固定/悬浮区分，宽屏与正文并排、窄屏为可关闭浮层；左右侧栏宽度均可调节并记忆，新窗口默认只呈现正文。图标按钮无常驻描边框，普通动作悬停浅底；左右栏入口悬停只改变图形颜色、选中仅一种底色强调，阅读设置图标按 Glossa 图标准则重绘。既有阅读、批注、同步与数据协议不变。

## 视觉与交互完善（2026-09-08）

以 Glossa 应用图标的黑白对比、圆角笔画和行间注释结构建立统一视觉系统。参考 Apple Books 的书库层级、Bear 的文字留白、Things 的控件细节及 Readwise Reader 的正文与批注关系，落实到现有基础阅读与同步范围。

- 默认主题使用纸白与墨灰，统一图标、圆角、间距、按钮层级、焦点及状态表达；保留深色、自定义主题、RTL、电子墨水与减少动效支持。
- 书库明确“继续阅读 → 全部书籍”的层级，改善书名、作者与进度的可读性，保留搜索、分组、导入、选择及同步操作。
- 阅读页保持正文优先，统一目录、页码导航、书签和批注侧栏；保留阅读引擎、已有用户排版设置及原文定位。
- 验收覆盖桌面和窄屏视觉、键盘及触控目标、阅读核心流程与相关回归检查；功能范围服从当前目标。

## 阅读配色（2026-09-09）

参考 Dark Reader 的柔灰纸面与炭黑夜色，重整内置主题为低饱和、明确对比的成对浅深配色，保留 Glossa 默认纸白/墨灰。以真实文字预览和可选择的预设呈现配色；常用方案优先，更多颜色可展开，自定义方案从当前配色开始。选择沿用既有主题持久化与系统明暗跟随，旧主题标识及用户自定义颜色保持兼容。

- 以 App 图标的圆角纸片、墨色和留白呈现配色：20px 圆角色卡居中字样、名称置于卡片下方，细轮廓与反色圆勾表示选中，轻微纸面层次提升质感；去除装饰横线与预览下划线。通用控件保留 12px 圆角，键盘、窄容器、RTL 和电子墨水可用；电子墨水关闭阴影和过渡。
- 内置正文、链接与次要文字经过对比度检查；不把配色偏好宣传为医学护眼效果，不引入滤镜或外部样式依赖。
- 自定义浅深方案可预览、保存与删除，取消不应用；改名保持原标识，同名冲突明确提示，保存失败保留草稿与已有偏好。

## macOS 界面适配（2026-09-09）

用户选择让界面更像 Mac 原生应用。保留 React/Tauri 与阅读、同步实现，在桌面宽度采用紧凑统一工具栏、系统界面字体和可收起的书库分类侧栏；阅读侧栏统一列表选中态与分段按钮。复用系统红黄绿按钮及现有拖动、全屏逻辑，不模拟窗口按钮。窄屏、RTL、电子墨水、深色与自定义阅读字体保持可用。验收包含分类导航、搜索快捷键、侧栏收起、阅读与批注流程及响应式布局。

## Glossa 品牌与上游归属（2026-09-09）

用户可见的产品名称、图标、窗口与安装元数据、帮助/反馈入口和导出署名统一为 Glossa。保留 Readest 原始版权、AGPL-3.0-or-later 许可证、第三方声明与 Git 历史；在关于页和仓库说明衍生关系、修改日期并提供 Glossa 源码与离线许可证。独立版本不得使用 Readest 的安装包或更新源。

实际 Readest Cloud 服务及其条款、存量本地/云数据路径、同步与 OAuth 协议标识、依赖名和历史工程路径保留准确含义；不为名称替换移动或删除用户书库数据。当前验收仍以本地 macOS 与 Web 为主，不发布应用，也不将上游移动端商店配置当作 Glossa 已上线版本。

系统层身份包含开发启动实际执行的二进制名称和安全存储条目名称。桌面安全存储以 Glossa 命名，正式版、开发版及测试身份隔离；旧 Readest 条目仅在新条目不存在时按需读取，复制成功后使用新条目，保留旧值。清除操作须防止旧值被再次恢复，权限拒绝或复制失败不得当作凭据不存在。已有书库目录、同步路径与账号协议保持不变；产品界面及导出入口使用 Glossa 名称，并继续兼容旧批注格式。

## 阅读交互与品牌图形（2026-09-09）

从 App 的圆端墨线和行间结构延伸可复用的 Glossa 矢量图标，统一书库导航、阅读工具栏、目录与笔记入口。阅读工具按位置与用途分组：返回与浏览在前，排版、书签、笔记在后；低频选择偏好和单书关闭操作通过有文字的菜单进入。侧栏在书籍身份下方直接切换目录和书签；切换无人工延迟，支持键盘导航。进度条拖动预览、松手定位并可取消，翻页与历史/章节导航分层。保留原生窗口行为、正文排版与触控边界，验收浅色、深色、窄屏、RTL 和电子墨水。

## 常见阅读字体（2026-09-09）

内置字体库改为常见系统字体预设：中文宋体、楷体、仿宋与黑体；西文 Times New Roman、Georgia、Arial、Helvetica 等；日文明朝与ゴシック；韩文 바탕 与 맑은 고딕；阿拉伯文 Traditional Arabic 与 Tahoma。以本地字体别名和通用字体回退适配平台，不捆绑商业字体、不自动请求外部字体服务。仅为缺少系统楷体的设备随应用附带 Arphic Public License 授权的 AR PL UKai CN 后备字体，保留完整字形、许可证与转换说明。默认按文档语言区分简繁中文、日文、韩文与阿拉伯文。保留系统字体扩展列表、自定义字体导入和删除功能；仅从 Glossa 字体候选及阅读字体链中排除用户指定的 100-SS Xian Song Ti、173-SSShanShuiSongTi、波本威士忌、寒蝉锦书宋、三极花朝体及其变体，其他系统与自定义字体保持可用。指定旧字体和旧内置默认值回退到常见预设；删除正在使用的导入字体时重置相应选择，失败时保留字体并反馈。保留共享同步数据协议，不卸载操作系统字体。验收包含字体扩展、导入/选择/删除/失败/取消、语言回退、旧配置与离线渲染。

## 字体配置交互（2026-09-09）

借鉴 Apple Books 的选项分层、Kobo 的阅读页调节和 Readwise Reader 的直接操作，重构现有字体配置。沿用 Glossa 的纸白/墨灰、主题变量、圆角及简明按钮，以样句体现字体差异，避免解释性长文。

- 主层按文档语言提供统一字体列表，“原书字体”与具体字体在同一列表选择，不再独立切换原书/自选。提供字号与 100–1000、步长 1 的字重滑杆和精确数字调节；字体列表原地展开、搜索并展示统一样句。“更多”仅保留对代码阅读有实际用途的等宽字体，保留字体管理和对应设置搜索入口。
- 根据用户后续精简要求，移除最小字号的界面、搜索入口和排版作用；旧字段仅为书籍/同步兼容保留，不自动清除或重写。移除三档字重按钮、独立衬线/无衬线类别切换及重复字族选择；通过一个正文字体入口直接选择，继续使用已保存的字族偏好，避免升级改变现有排版。
- 中文样句为“微雨从东来，好风与之俱”，西文为“Sunt lacrimae rerum et mentem mortalia tangunt.”；繁体界面对应繁体字形，其他文字采用同样诗意的原创短句。
- 桌面宽屏阅读设置所有标签页共用右侧面板（RTL 随方向镜像），切换字体、排版、主题、行为、语言、云同步、模型服务与无障碍设置时保持位置和尺寸一致；窄屏沿用响应式弹层。保留正文可见区域；调整沿用现有即时排版与本书/全局保存协议。打开面板不重复保存未改变的设置，保留导入字体、删除回退和旧字体迁移。
- 验收包含选择/搜索/返回/重置、错误重试、保存范围、外部字体变化、键盘、深色、窄屏、RTL 和电子墨水；不改变阅读引擎、用户书籍或安装版应用。
