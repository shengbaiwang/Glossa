# Glossa 执行计划

## 1. 目标

为研究人员和学生构建一个 AI 辅助阅读器，在阅读 PDF、网页、EPUB 等内容时提供划词解释、问答、翻译、摘要和笔记，并确保回答能联系阅读上下文、引用原文、跳回出处。

当前目标不是一次完成全平台产品，而是验证以下核心假设：

> 当 AI 自动理解当前阅读位置、只使用相关上下文并给出可检查来源时，用户会更容易理解复杂材料，并愿意在真实阅读中持续使用它。

## 2. 用户与场景

### 目标用户

- 研究人员：阅读论文、专著、技术报告，追踪论点、证据、术语和引用。
- 学生：阅读教材、课程材料和文献，解释概念、翻译内容、总结章节并整理笔记。

### 核心场景

1. 遇到术语或复杂段落，选中后立即解释或翻译。
2. 针对当前内容提问，AI 自动理解阅读位置，无需重复描述背景。
3. 询问“这和前文有什么关系”，AI 检索已读内容并给出出处。
4. 读完章节后生成基于原文的结构化摘要。
5. 将有价值的回答保存为与原文绑定的笔记。

## 3. 产品范围

### 最小可行产品（MVP）

- 导入并阅读 EPUB。
- 选中文字后执行“解释”和“翻译”。
- 在阅读侧边栏中自由提问并连续追问。
- AI 使用选区、当前位置、当前章节和已读前文回答。
- 生成当前章节摘要。
- 每个事实性回答附带可点击来源。
- 点击来源跳回原文并临时高亮。
- 将回答片段保存为笔记。
- 本地保存阅读状态、来源、对话和笔记。
- 使用用户自己的模型 API Key，不建设账号和云服务。

### MVP 之后

- 数字版 PDF 阅读与页内引用。
- 网页导入、正文提取与稳定定位。
- 混合关键词和语义检索。
- 扫描 PDF、OCR、公式和复杂表格解析。
- Web 客户端和移动端。
- 加密同步、本地 embedding 和本地 LLM。
- 多文档联合研究和笔记导出。

### 明确不在验证阶段建设

- 社交、分享和多人协作。
- 知识图谱可视化。
- 开放式多 Agent 工作流。
- 推荐系统、内容市场和插件生态。
- 自建模型、微服务平台或独立向量数据库。
- 全功能参考文献管理器。

## 4. 产品体验

### 核心闭环

```text
阅读 → 产生疑问 → 划词或提问 → 获得有出处的回答 → 返回原文继续阅读
```

### 最简界面

1. Readest 选区菜单新增“问 Glossa”。
2. 右侧打开 AI 面板，不离开当前书页。
3. 快捷动作仅保留“解释”“翻译”“联系前文”。
4. 输入框支持自由问题和连续追问。
5. 面板顶部显示本次上下文范围，例如“选区 + 当前章节 + 已读前文”。
6. 回答按段落显示来源标签；点击来源跳回原文。
7. 用户可以把回答片段保存为笔记，笔记继续绑定原文来源。

### 防剧透规则

- 默认只使用当前位置之前的内容。
- 如果问题必须依赖后文，提示用户是否允许检索全书。
- 用户明确开启“允许参考全书”后才能使用未读内容。

## 5. 技术方案

### 5.1 实验载体

- 基于 Readest 建立个人验证版本。
- 锁定一个明确的上游 commit，记录上游来源和许可证。
- 尽量通过独立 feature/module 集成 Glossa，减少直接修改阅读核心。
- 定期同步上游只在确有需要时进行，避免验证阶段消耗在合并维护上。

### 5.2 统一阅读适配器

AI 核心只依赖统一接口，不直接依赖 EPUB、PDF 或网页实现：

```ts
interface DocumentAdapter {
  getSelection(): Promise<SelectedText | null>
  getCurrentLocation(): Promise<DocumentLocation>
  getVisibleText(): Promise<SourceSegment[]>
  getSectionText(sectionId: string): Promise<SourceSegment[]>
  search(query: string, scope: SearchScope): Promise<SourceSegment[]>
  goTo(anchor: SourceAnchor): Promise<void>
}
```

计划中的格式实现：

- EPUB：Readest/foliate-js，使用章节路径、CFI 和文本引用组合定位。
- PDF：PDF.js，使用页码、文本位置、矩形区域和文本引用组合定位。
- 网页：正文快照，使用 URL、DOM/CSS 路径、TextQuote 和 TextPosition 组合定位。

### 5.3 统一来源锚点

```ts
type SourceAnchor = {
  documentId: string
  format: "epub" | "pdf" | "web"
  sectionId?: string
  cfi?: string
  page?: number
  rects?: number[][]
  url?: string
  textPosition?: { start: number; end: number }
  quote: {
    exact: string
    prefix?: string
    suffix?: string
  }
}
```

定位恢复顺序：

1. 格式原生定位：CFI、页码区域或网页结构定位。
2. 文本位置。
3. `exact + prefix + suffix` 精确匹配。
4. 有限制的模糊匹配。
5. 无法精确定位时降级为章节级或页级来源，并明确标示。

### 5.4 AI 请求流程

```text
用户问题/快捷动作
  → 获取选区与当前位置
  → 收集邻近文本和当前章节
  → 在已读范围执行全文搜索
  → 生成带 sourceId 的最小上下文包
  → 调用模型并流式返回结构化回答
  → 校验 sourceId 白名单
  → 从本地读取真实引文
  → 显示回答、来源和跳转入口
```

模型回答协议：

```ts
type GlossaAnswer = {
  status: "answered" | "insufficient_evidence"
  paragraphs: Array<{
    text: string
    sourceIds: string[]
    basis: "document" | "inference" | "external"
  }>
  followups: string[]
}
```

### 5.5 检索策略

MVP：

- 当前选区和前后段落直接进入上下文。
- 当前章节拥有最高优先级。
- 复用 Readest 的全文搜索或建立简单本地全文索引。
- 问题拆成少量关键词，在已读范围内召回相关段落。
- 不创建独立向量数据库。

后续升级条件：

- 固定评测集的关键词检索 `Recall@10` 低于 85%；或
- 日常使用中超过 20% 的跨章节问题因为同义表达找不到相关原文。

达到条件后加入 embedding，与关键词结果进行混合排序。单机验证阶段向量保存在本地数据库，数据规模扩大后再评估 ANN 或服务端向量库。

### 5.6 数据与隐私

- 文档、来源锚点、阅读进度、笔记和对话保存在本地。
- API Key 保存在系统钥匙串。
- 发往模型的上下文默认限制为与问题相关的原文片段。
- 日志只保存错误类型、延迟和 token 使用量，不保存完整正文。
- 所有 AI 缓存绑定 `documentHash + modelId + promptVersion`。

## 6. 可执行任务拆解

### A. 项目基线

- [x] A01 导入 Readest 上游源码并记录上游 commit。
- [x] A02 保留并记录 Readest AGPL-3.0 许可证和第三方声明。
- [x] A03 在本机启动桌面开发版本。
- [x] A04 跑通上游已有 lint、类型检查和测试。
- [x] A05 建立 Glossa 功能开关和模块目录。
- [x] A06 准备一份合法可用于开发测试的 EPUB 样本。

验收：未增加 AI 功能时，原有阅读流程能够稳定运行；Glossa 功能可以独立开启和关闭。

### B. 阅读上下文适配

- [x] B01 定位 Readest 的选区事件和选区文本接口。
- [x] B02 获取当前章节、阅读位置和已读进度。
- [x] B03 获取当前可见内容及前后段落。
- [x] B04 封装 EPUB `DocumentAdapter`。
- [x] B05 定义并序列化 `SourceAnchor`。
- [x] B06 实现从锚点跳回 EPUB 原文。
- [x] B07 实现跳转后的临时高亮和返回阅读位置。
- [x] B08 为选区、定位和跳转增加测试（单元、真实浏览器重排和 macOS Tauri WebView 均已通过）。

验收：同一 EPUB 在改变窗口大小、字体和分页模式后，测试锚点仍能恢复到正确原文；测试集成功率不低于 99%。

### C. AI 交互界面

- [x] C01 在选区工具栏增加“问 Glossa”。
- [x] C02 建立可折叠的阅读侧边栏。
- [x] C03 实现解释、翻译、联系前文三个快捷动作。
- [ ] C04 实现自由输入、连续追问和取消请求（部分完成：支持取消与新请求替换旧请求；自由输入和连续追问留待下一轮）。
- [x] C05 实现流式回答、加载、错误和证据不足状态。
- [x] C06 显示当前上下文范围与防剧透状态。
- [x] C07 实现来源标签、引用预览和点击跳转。
- [ ] C08 支持把回答段落保存为笔记。

验收：用户无需离开阅读页即可完成选区提问和自由问答；请求可以取消；所有文档事实段落均显示至少一个有效来源。

### D. 模型接入与回答约束

- [x] D01 定义模型无关的 `AIProvider` 接口。
- [ ] D02 接入第一个支持流式和结构化输出的模型供应商。
- [ ] D03 将 API Key 存储到系统钥匙串。
- [x] D04 定义并校验 `GlossaAnswer` schema。
- [x] D05 实现 sourceId 白名单校验。
- [ ] D06 实现证据不足、推断和外部知识标记。
- [ ] D07 实现超时、取消、限流和一次性修复重试。
- [ ] D08 显示单次请求 token/费用估算，便于个人验证。

验收：模型不能引用未提供的 sourceId；非法结构不会进入 UI；密钥不出现在仓库、日志和前端存储中。

### E. 检索与上下文组装

- [ ] E01 统一清洗章节文本并保留段落结构。
- [x] E02 为每个文本片段分配稳定 sourceId 和锚点。
- [x] E03 实现当前选区与邻近段落上下文。
- [ ] E04 实现当前章节上下文。
- [ ] E05 实现已读范围过滤。
- [ ] E06 接入全文关键词搜索。
- [ ] E07 实现候选去重、排序和 token 预算。
- [ ] E08 实现对话历史压缩。
- [ ] E09 缓存章节摘要并提供失效机制。

验收：默认回答不引用未读后文；上下文包不包含无关全文；固定问题集 `Recall@10` 不低于 85%。

### F. 摘要、翻译与笔记

- [ ] F01 实现基于选区的解释和翻译。
- [ ] F02 实现章节摘要：核心观点、证据、概念、未解决问题。
- [ ] F03 摘要中的每个核心观点绑定来源。
- [ ] F04 保存、编辑和删除 AI 辅助笔记。
- [ ] F05 笔记绑定用户选区、AI 回答和原文来源。
- [ ] F06 提供 Markdown/JSON 导出。

验收：摘要可追溯到原文；保存后的笔记能够重新打开对应原文；导出不丢失来源信息。

### G. PDF 与网页扩展

- [ ] G01 封装 PDF `DocumentAdapter`。
- [ ] G02 提取数字 PDF 文本、页码和坐标。
- [ ] G03 实现 PDF 引用预览与页内区域跳转。
- [ ] G04 建立复杂版面和扫描 PDF 的降级提示。
- [ ] G05 评估并按需接入本地 OCR/Docling。
- [ ] G06 实现网页正文抓取、清洗和本地快照。
- [ ] G07 封装网页 `DocumentAdapter`。
- [ ] G08 实现网页文本引用恢复和原 URL 跳转。

验收：数字 PDF 引用跳转成功率不低于 97%；网页内容变化后可以通过文本引用恢复或明确显示定位降级。

### H. 平台扩展

- [ ] H01 将 AI 核心和领域协议从桌面 UI 中分离。
- [ ] H02 验证 Web 端文件访问、索引和密钥策略。
- [ ] H03 建立响应式阅读与 AI 面板布局。
- [ ] H04 验证 Tauri iOS/Android 构建。
- [ ] H05 针对触控选择、软键盘和移动侧栏调整交互。
- [ ] H06 需求成立后设计加密同步协议。

验收：核心回答 schema、来源锚点和检索逻辑能够跨平台复用；不得为平台适配复制一套 AI 核心。

### I. 质量与评测

- [ ] I01 建立包含理论书、技术书和叙事作品的 EPUB 测试集。
- [ ] I02 建立当前段落、跨章节、无答案、冲突证据和剧透问题集。
- [ ] I03 建立引用支持度人工评分流程。
- [ ] I04 记录回答延迟、检索召回、跳转成功率和成本。
- [ ] I05 建立文档提示注入和恶意 EPUB 测试。
- [ ] I06 建立两周真实阅读验证记录。

验收：达到第 8 节所列的 MVP 决策门槛，并能够复现评测结果。

## 7. 里程碑

### M0：项目基线（已完成）

包含：A01–A06。

交付结果：Readest 开发版可运行，Glossa 模块有清晰入口，测试样本准备完成。

### M1：EPUB 垂直闭环

包含：B01–B08、C01–C07、D01–D07、E01–E04。

交付结果：

```text
选中文字 → 问 Glossa → 得到带来源回答 → 点击来源 → 回到原文
```

这是第一个需求验证版本。在此闭环稳定前，不进入 PDF、网页或移动端。

### M2：可日常使用的 EPUB MVP

包含：C08、D08、E05–E09、F01–F06、I01–I04。

交付结果：支持已读范围问答、连续追问、章节摘要和可追溯笔记，可以进行两周真实阅读验证。

### M3：多格式验证

包含：G01–G08、I05。

交付结果：支持数字 PDF 和网页；复杂 PDF 有明确降级路径。

### M4：跨平台与同步决策

包含：H01–H06。

前置条件：MVP 达到持续使用指标。未达到时不得因为“路线图完整”进入此里程碑。

## 8. 验收标准与继续投入门槛

### 工程指标

| 指标 | MVP 目标 |
|---|---:|
| EPUB 引用跳转成功率 | ≥ 99% |
| 数字 PDF 引用跳转成功率 | ≥ 97% |
| 关键词检索 Recall@10 | ≥ 85% |
| 引用确实支持对应回答 | ≥ 90% |
| 无答案问题正确返回证据不足 | ≥ 85% |
| 默认回答引用未读后文 | 0 次 |
| 本地检索 P95 | < 300ms |
| 普通回答首段出现时间 | < 3s，不含供应商异常 |

### 产品指标

连续两周使用三类真实材料进行验证：

- 至少完成 10 次真实阅读会话。
- 至少 70% 的 AI 交互能够帮助用户继续阅读。
- 两周后用户仍会主动调用 Glossa。
- 每次阅读会话平均主动调用不少于 2 次。
- 用户能够明确指出 Glossa 相比复制内容到通用聊天工具的优势。

满足以上条件后，才继续投入语义检索、复杂 PDF、Web、移动端和同步。否则优先修正交互、上下文和引用，不扩张功能面。

## 9. 主要风险与应对

| 风险 | 应对 |
|---|---|
| Readest 上游结构复杂 | Glossa 保持独立模块，通过适配层接入 |
| AGPL 影响未来分发 | 当前只用于个人验证；分发前重新审查并决定重建或开源 |
| 模型编造引用 | 只接受白名单 sourceId，引文由本地读取 |
| EPUB 重排导致位置失效 | CFI + 文本位置 + TextQuote 多重定位 |
| PDF 阅读顺序错误 | 数字 PDF 先行，复杂版面启用可选高精度解析 |
| 上下文太长、成本过高 | 分层检索、token 预算、摘要缓存和最小发送 |
| 过早扩展平台 | M1/M2 通过前冻结 PDF、网页和移动端 |
| 文档提示注入 | 文档内容始终视为数据，不允许触发工具或覆盖系统规则 |

## 10. 决策记录

| 日期 | 决策 | 原因 |
|---|---|---|
| 2026-08-13 | 使用 Readest 作为个人验证载体 | 最大化复用阅读能力，快速验证 AI 阅读体验 |
| 2026-08-13 | 桌面端 EPUB 先行 | 最短路径验证选区、上下文、引用和跳转闭环 |
| 2026-08-13 | 默认只检索已读内容 | 防止剧透并让上下文范围符合阅读过程 |
| 2026-08-13 | MVP 不使用独立向量数据库 | 降低复杂度，先用真实评测决定是否需要语义检索 |
| 2026-08-13 | 回答只引用白名单 sourceId | 从协议层防止伪造引文与位置 |
